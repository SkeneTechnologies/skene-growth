---
name: skene-analyze
description: Analyze current codebase for PLG opportunities. Scans tech stack, detects existing growth features, identifies revenue leakage, and updates the feature registry.
---

# Analyze Codebase

1. Invoke the `analyze-codebase` skill.
2. Run `uvx skene analyze --help` first to discover available flags.
3. **Run autonomously.** If the CLI succeeds, present results and suggest `/skene-plan`. If it fails, report the error. Do NOT generate your own analysis.
